# Phase 2 Tasks

- [x] Change `settle.ts` logic so Faders win the Escrow + Backer pool if the goal fails.
- [x] Create `src/core/db.ts` to manage persistence (using `localStorage` + `storage` events for cross-tab sync to simulate multiplayer).
- [ ] Create simple Screen/Router logic in `src/App.tsx`.
- [ ] Create `Onboarding` screen (Username + PFP).
- [ ] Create `CreateMarket` screen (Set goal, deadline, initial escrow).
- [ ] Update `Pot.tsx` to dynamically scale based on Escrow + Backing + Doubt pool sizes.
- [ ] Update `verify.ts` AI prompt to include the deadline expiration check.
